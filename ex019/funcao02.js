/*A função apresentada é chamada 'soma' e aceita dois parâmetros, 'n1' e 'n2', que têm valores padrão de 0 caso nenhum valor seja fornecido ao chamar a função. Essa função retorna a soma dos dois parâmetros. */

function soma(n1=0, n2=0){
    return n1 + n2
}

console.log(soma(7))